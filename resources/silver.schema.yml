resources:
  schemas:
    silver:
      name: ${var.silver_schema}
      catalog_name: ${var.data_catalog}
      comment: Validated, typed, deduplicated business records for the equity research system.
      lifecycle:
        prevent_destroy: true