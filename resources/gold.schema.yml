resources:
  schemas:
    gold:
      name: ${var.gold_schema}
      catalog_name: ${var.data_catalog}
      comment: Reusable analytical metrics for the equity research system.
      lifecycle:
        prevent_destroy: true
