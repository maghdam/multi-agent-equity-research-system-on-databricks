name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  lint-and-test:
    name: Lint and test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt -r requirements-dev.txt

      - name: Ruff lint
        run: ruff check .

      - name: Credential-free AI evaluation gates
        run: |
          python -m unittest discover -s tests -p "test_tool_access_requests.py"
          python -m unittest discover -s tests -p "test_structured_data_tools.py"
          python -m unittest discover -s tests -p "test_retrieval_evaluation.py"
          python -m unittest discover -s tests -p "test_live_retrieval_evaluation.py"
          python -m unittest discover -s tests -p "test_controlled_evaluation_fixtures.py"
          python -m unittest discover -s tests -p "test_mlflow_evaluation.py"
          python -m unittest discover -s tests -p "test_mlflow_runtime_spans.py"
          python -m unittest discover -s tests -p "test_mlflow_tracing.py"

      - name: Offline unit and contract tests
        run: python -m unittest discover -s tests -p "test_*.py"
